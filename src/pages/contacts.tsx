import ContactsComponent from "@/views/Contacts/Contacts";
import React from "react";

const Contacts = () => {
  return (
    <div>
      <ContactsComponent />
    </div>
  );
};

export default Contacts;
